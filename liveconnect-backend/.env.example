# --- Server ---
PORT=4000
# Dozvoljeni frontend origini (zarezom)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:5173

# --- DB ---
# Dev lokalno (Compose koristi db host "db"):
DATABASE_URL=postgresql://postgres:123456@localhost:5433/liveconnect?schema=public

# --- Auth cookie ---
JWT_SECRET=change_me
COOKIE_NAME=lc_token
# Za DEV (http):
COOKIE_SECURE=false
COOKIE_SAMESITE=lax
# Za produkciju (https, npr. app.example.com & api.example.com):
# COOKIE_SECURE=true
# COOKIE_SAMESITE=none
# COOKIE_DOMAIN=.example.com
